"use client";

import { useAppSelector } from "@/lib/hook";
import React from "react";

const Container = () => {
  return (
    <div className="container">
      <section>
        <h2 className="section-title">최원석 사원 작업 공간</h2>
      </section>
    </div>
  );
};

export default Container;
